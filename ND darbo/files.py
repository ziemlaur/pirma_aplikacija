# 1. What is the difference between shutil.copy() and shutil.copytree()?
# copy() only copies one file/folder, copytree() copies whole directory with files and folders inside
# 2. What function is used to rename files?
# move
# 3. What is the difference between the delete functions in the send2trash and shutil modules?
# delete function deletes file without recovery option, send2trash moves files to trash bin
# 4. ZipFile objects have a close() method just like File objects’ close() method. 
# What ZipFile method is equivalent to File objects’ open() method?
# ZipFile.open()
